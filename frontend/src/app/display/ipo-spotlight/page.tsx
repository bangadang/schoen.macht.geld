import IpoSpotlightClient from './ipo-spotlight-client';

export const metadata = {
  title: 'IPO Spotlight | Schön. Macht. Geld.',
  description: 'Neue Aktien im Rampenlicht',
};

export default function IpoSpotlightPage() {
  return <IpoSpotlightClient />;
}